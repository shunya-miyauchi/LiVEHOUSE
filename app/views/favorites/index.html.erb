<div class="d-flex flex-row justify-content-center">
  <div class="nav flex-column">
    <div>
      検索窓をここに配置
    </div>

    

    <ul class="nav flex-column">
      <% @favorite_livehouses.each do |livehouse| %>
        <li class="nav-item">
          <%= link_to "#{livehouse.name}", favorite_path(livehouse.id) %>
          <span id="favorite_<%= livehouse.id %>"><%= render 'events/livehouse', { livehouse: livehouse } %></span>
        </li>
      <% end %>
    </ul>
  </div>

  <div>
    <% if @favorite_livehouses.blank? %>
      <div>ライブハウスをお気に入り登録して下しさい。</div>
    <% else %>
      <div>ライブハウスを選択してください。</div>
    <% end %>
  </div>


</div>