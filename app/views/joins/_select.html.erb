<% if user_signed_in? %>
  <% if current_user.joins_by?(event.id) %>
    <%= link_to '不参加', join_path(id: event.id), method: :delete, remote: true  %>
  <% else %>
    <%= link_to '参加', joins_path(id: event.id), method: :post, remote: true %>
  <% end %>
<% end %>